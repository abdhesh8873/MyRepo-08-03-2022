---
- hosts: all
  become: yes
  tasks:
  - name: i want to install webserver on ansible ubuntu nodes
    apt:
      name: apache2
      state: present
    then: ansible_os_family =="Debian"
  - name: i want to make always on apache2 on ubuntu nodes
    apt: 
      name: apache2
      state: started

